
result:

```bash
$ python src/difference.py
difference= {'Google Fi Wireless', 'Google One', 'Classic Sites', 'Fitbit', 'My Maps', 'Business messages', 'Recorder', 'Google Feedback'}
len(difference)= 8
```
